stages:
  - build

variables:
  DOCKER_REPOSITORY: hs110influx
  DOCKER_REGISTRY: docker.jfrog.wouterstemgee.be
  
docker_build:
  image: docker:latest
  stage: build
  services:
    - docker:dind
  before_script:
    - docker login -u gitlab-ci -p gitlab-ci ${DOCKER_REGISTRY}
  script:
    - docker build --pull --rm -f "Dockerfile" -t ${DOCKER_REGISTRY}/${DOCKER_REPOSITORY}:latest "."
    - docker push ${DOCKER_REGISTRY}/${DOCKER_REPOSITORY}:latest
  only:
    - master

docker_run:
  stage: build
  script:
    - docker run --rm -it ${DOCKER_REPOSITORY}:latest
